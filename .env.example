# V2Ray 服务器配置环境变量
# 复制此文件为 .env 并修改相应的值
# 使用方法: cp .env.example .env

# ============================================
# 必需配置
# ============================================

# 域名（必须配置，用于 TLS 证书）
DOMAIN=example.com

# WebSocket 路径（默认: /v2ray，建议使用随机路径）
# 留空将自动生成随机路径
WS_PATH=

# ============================================
# 可选配置
# ============================================

# 邮箱地址（用于 Let's Encrypt 证书通知，可选）
# 留空将使用 admin@${DOMAIN}
EMAIL=

# V2Ray 监听端口（默认: 443，通常不需要修改）
V2RAY_PORT=443

# V2Ray 内部端口（默认: 10000，Nginx 代理使用）
V2RAY_INTERNAL_PORT=10000

# V2Ray 日志级别（可选: debug, info, warning, error, none）
# 默认: warning
LOG_LEVEL=warning

# 是否自动生成随机 WebSocket 路径（默认: true）
# 如果设置为 false，将使用 WS_PATH 的值，如果 WS_PATH 为空则使用 /v2ray
AUTO_GENERATE_WS_PATH=true

# UUID（留空将自动生成）
# 如果已有 UUID，可以在这里设置
UUID=